{
  "name": "experimental-dts-node-next",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "packageManager": "yarn@4.5.0",
  "devDependencies": {
    "@microsoft/api-extractor": "^7.47.9",
    "prettier": "^3.3.3",
    "rimraf": "^6.0.1",
    "tsup": "^8.3.0",
    "typescript": "^5.6.2"
  },
  "type": "module",
  "scripts": {
    "build:node-next": "tsup --tsconfig tsconfig.json",
    "build:bundler": "tsup --tsconfig tsconfig.bundler.json",
    "clean": "rm -rf dist"
  }
}
